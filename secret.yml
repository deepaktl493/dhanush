name: Use Secret

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print secret (masked)
        run: echo "API Key is $API_KEY"
        env:
          API_KEY: ${{ secrets.API_KEY }}
